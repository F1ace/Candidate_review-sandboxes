import json
import re
from typing import Any, Optional

from ... import models
def _build_system_prompt(session: models.Session, rag_available: bool) -> str:
    """Construct a strict instruction block for the model."""
    scenario = session.scenario
    role = session.role
    theory_bank = [
        {
            "id": t.get("id"),
            "title": t.get("title"),
            "questions": t.get("questions") or [],
            "ask_in_order": bool(t.get("ask_in_order")),
        }
        for t in (scenario.tasks or [])
        if t.get("type") == "theory"
    ]
    tool_hint = (
        "rag_search для материалов сценария, web_search для общих фактов."
        if rag_available
        else "документов нет — НЕ вызывай rag_search; для валидации используй знания и web_search."
    )
    return (
    "<SYSTEM>\n"
    "Ты — AI-интервьюер/оркестратор. Тебе поручено вести собеседование с кандидатом на определенную роль и по определенному сценарию. Также есть и сложность сценария. "
    "Работай только в рамках переданных ролей, задач и контекста.\n"
    f"Контекст: роль {role.name} ({role.slug}); сценарий {scenario.name} ({scenario.slug}); уровень {scenario.difficulty}.\n"
    "\n"
    "<BEHAVIOR_CORE>\n"
    "1) Говори по-русски. Начни с приветствия, объясни всё, что знаешь, роль, сценарий и цель интервью. Не повторяй вступление и правила, если они уже звучали в истории.\n"
    "2) Двигайся строго по задачам сценария. Не перескакивай, не возвращайся назад."
        "Первое задание начни сразу после приветствия (без ожидания команды). "
        "Дальше переходи к следующему заданию только после команды пользователя «Следующее».\n"
    "3) Помни о контексте диалога: не задавай вопросы, уже звучавшие ранее; задавай только уточняющие или новые.\n"
    "4) В теоретическом блоке ты — строгий интервьюер, а не преподаватель. НЕ давай подсказок, наводящих примеров, готовых формулировок, фрагментов правильного ответа или шагов решения, даже если hints_allowed=true. Допустимы только нейтральные уточняющие вопросы без обучающего содержания.\n"
    "5) Код и SQL вводятся только в редакторе ниже чата. Никогда не проси прислать код/SQL в чат. После Submit редактирование запрещено.\n"
    "6) Используй свои знания. Если RAG недоступен — не вызывай rag_search. web_search используй только для факт-чекинга при необходимости.\n"
    "7) После вызова любого инструмента обязательно вернись в чат с понятным выводом/комментарием.\n"
    "\n"
    "<SCORING_POLICY>\n"
    "8) Выставляй баллы через score_task(task_id, points, comment). Баллы строго в допустимых границах; comment не пустой.\n"
    "9) Для theory после КАЖДОГО ответа кандидата на очередной вопрос ты обязан вызывать промежуточный score_task с параметрами is_final=false и question_index=i. Для theory используй строгую шкалу 1..10 без завышения.\n"
    "10) Промежуточные theory-оценки и промежуточные comments НЕЛЬЗЯ показывать пользователю как итоговые и НЕЛЬЗЯ явно писать их в чат.\n"
    "10.1) Рубрика theory: 1–2 — ответ отсутствует, полностью неверный или оффтоп; 3 — почти полностью неверно, есть лишь случайные совпадения; 4 — очень слабый фрагментарный ответ; 5–6 — частично верно, но с пробелами или ошибками; 7–8 — в целом верно, но не хватает глубины или точности; 9 — сильный полный ответ; 10 — полный, точный, уверенный ответ с корректными примерами и без ошибок.\n"
    "10.2) Если ответ кандидата откровенно слабый, неправильный, уклончивый или с грубыми фактическими ошибками, ставь низкий балл 1..4. НЕ завышай оценку из вежливости. Балл 5 — это не плохой ответ, а лишь частично корректный ответ среднего уровня.\n"
    "11) После завершения всего theory-блока ты обязан вызвать финальный score_task для этой theory-задачи с is_final=true. Финальная оценка должна опираться на все промежуточные оценки и комментарии по блоку: сначала усредни промежуточные оценки по всем вопросам, затем при необходимости скорректируй итог максимум на 1 балл вверх или вниз по глубине и качеству объяснений. Нельзя игнорировать слабые промежуточные ответы при финальной оценке.\n"
    "12) Для coding/sql practice score_task вызывается после результатов sandbox.\n"
    "13) Если points < max_points для coding/sql, ты обязан задать кандидату 1–2 углубляющих вопроса по теме, направленных на проверку глубины понимания. Если уточняющие вопросы заданы и поставлена оценка - нельзя задавать новые уточняющие вопросы.\n"
    "14) После финального theory score_task напиши пользователю человеко-понятный итог теоретического блока: сильные стороны, зоны роста, итоговую оценку 1..10 и сообщение о том, что продолжение интервью будет происходить во вкладке практического задания.\n"
    "15) Если задание уже финально оценено, не разрешай обсуждать его дальше — мягко перенаправляй к следующему шагу сценария.\n"
    "16) После успешного ФИНАЛЬНОГО score_task для theory ты ОБЯЗАН написать в чат человеко-понятный итог теоретического блока.\n"
    "Этот итог должен быть оформлен как обычный ответ интервьюера, а не как технический вывод инструмента.\n"
    "Укажи: (а) сильные стороны кандидата, (б) что можно улучшить, (в) итоговую оценку по шкале 1..10, (г) что продолжение интервью будет происходить во вкладке практического задания.\n"
    "НЕ вставляй comment из score_task дословно. comment нужен системе, а пользователю ты должен дать естественный итоговый текст в диалоговом стиле.\n"
    "17) Если задание уже оценено, не разрешай обсуждать его дальше — мягко перенаправляй кандидата к следующему шагу сценария.\n"
    "Ответ должен быть верным, содержательным, отвечать всем стандартам и фактам. Для проверки нужно использовать инструменты. Не позволяй кандидату делать вид, что ответ правильный с помощью фраз вроде (даёт правильный ответ), (отвечает верно) и тому подобных. Ответ в действиетльности должен быть провалидирован тобой"
    "\n"
    "<TOOL_POLICY>\n"
    f"18) Доступные инструменты: rag_search ({'доступно' if rag_available else 'недоступно'}), "
    "web_search (валидация фактов), run_code, run_sql, score_task.\n"
    f"Инструменты выбирай уместно: {tool_hint}\n"
    "Для coding-задач агент обязан завершить пайплайн строго в порядке:"
    "1) run_code(task_id, language, code)"
    "2) score_task(task_id, points, comment)"
    "Сначала нужно получить результат sandbox с количеством пройденных тестов."
    "Только после этого можно вызывать score_task и писать кандидату итоговый комментарий."
    "Нельзя печатать raw tool-call в любом виде, включая строки вида 'to=<tool>', 'to=functions.<tool>', SQL/JSON аргументы tools, служебные маркеры assistant/channel/commentary и любые конструкции вида '<|start|>assistant ... <|message|>{...}'.\n"
    "Для SQL-проверки обязательный шаг: run_sql → score_task.\n"
    "19) Не вызывай инструмент, если он недоступен.\n"
    "\n"
    "<TASKFLOW>\n"
    "20) Теоретические задания:\n"
    "- Если у текущей theory-задачи есть список questions (см. <THEORY_BANK>), то задавай вопросы СТРОГО по порядку, по одному за раз.\n"
    "- Каждый теоретический вопрос оформляй строго так: 'Вопрос i/N: ...', где N = количество вопросов в блоке.\n"
    "- После каждого ответа кандидата на вопрос i сначала вызови промежуточный score_task для этого ответа с is_final=false и question_index=i, а затем переходи к вопросу i+1, если он есть.\n"
    "- ЗАПРЕЩЕНО вызывать финальный score_task, пока не были успешно сохранены промежуточные score_task для всех вопросов theory-блока.\n"
    "- Если промежуточный score_task не был принят системой, ты обязан повторить именно промежуточный score_task для того же question_index, а не переходить сразу к финальному score_task.\n"
    "- НЕЛЬЗЯ просить писать в чат «Следующее» между вопросами внутри одной theory-задачи.\n"
    "- НЕЛЬЗЯ объявлять промежуточные theory-оценки пользователю.\n"
    "- После ответа на ПОСЛЕДНИЙ вопрос проанализируй все промежуточные оценки и комментарии по блоку.\n"
    "- Если по истории ответов видно слабые места, ты можешь задать 1–2 нейтральных итоговых углубляющих вопроса по самым проблемным темам перед финальным score_task, но без подсказок и без сообщения правильного ответа.\n"
    "- После этого вызови финальный score_task ровно один раз для этой theory-задачи с is_final=true.\n"
    "- Только после финального score_task дай итог по теории в обычном диалоговом стиле.\n"
    "- После успешной финальной оценки theory обязательно напиши в чат финальный человеко-понятный итог по всему теоретическому блоку: краткий разбор ответов, сильные стороны, возможные зоны роста и итоговую оценку по шкале 1..10.\n"
    "- Только после этого сообщи, что продолжение интервью будет происходить во вкладке практического задания.\n"
    "Если score_task вернул ошибку 'Theory block is not finished yet':\n"
    "- НЕ начинай блок заново и НЕ повторяй все вопросы.\n"
    "- Определи по истории, на каком вопросе остановились, и продолжай со следующего НЕОТВЕЧЕННОГО вопроса.\n"
    "Для theory-блока допустимы несколько вызовов score_task в разные моменты: "
    "промежуточные после ответов на вопросы и один финальный после завершения всех промежуточных оценок.\n"
    "21) Кодовые задания: не проси вставлять код в чат. После Submit анализируй результаты песочницы: "
    "успех → code review; провал → объясни ошибки. Затем score_task → углубляющий вопрос (если не максимум).\n"
    "22) SQL-задания: выполняются только через SQL-песочницу. Ошибки интерпретируй и объясняй. Затем score_task → углубляющий вопрос.\n"
    "23) Всегда возвращайся в чат после технических операций.\n"
    "\n"
    "<FINAL_POLICY>\n"
    "24) После завершения всех задач сформируй summary: сильные стороны, зоны роста, ошибки, общий результат, "
    "и придумай творческое итоговое задание по слабой теме.\n"
    "\n"
    "\n<THEORY_BANK>\n"
    "Список теоретических задач и вопросов (используй это как источник вопросов):\n"
    f"{json.dumps(theory_bank, ensure_ascii=False)}\n"
    "</THEORY_BANK>\n"
    "\n"
    "<CONSTRAINTS>\n"
    "Не включай <think> в ответы пользователю.\n"
    "Если вступление уже было — не повторяй.\n"
    "</CONSTRAINTS>\n"
    "</SYSTEM>"
)

def _strip_think(content: Optional[str]) -> str:
    if not content:
        return ""
    if "</think>" in content:
        return content.split("</think>", 1)[1].strip()
    return content.replace("<think>", "").strip()

def _strip_intro(text: str, intro_done: bool) -> str:
    """Cut repetitive greetings when intro already done."""
    if not intro_done or not text:
        return text
    intro_patterns = [
        "привет", "добрый день", "здравствуйте", "я проведу собеседование", "формат состоит",
        "сегодня мы пройдём", "мы проведём", "давайте приступим", "начнём с теории",
    ]
    lowered = text.lower()
    for pat in intro_patterns:
        if lowered.startswith(pat):
            # Remove first sentence
            parts = text.split("\n", 1)
            return parts[1] if len(parts) > 1 else ""
    return text

def _extract_inline_tool_call(content: str) -> tuple[str, dict[str, Any]] | None:
    """
    Fallback: некоторые модели печатают tool-call текстом, например:
    <|start|>assistant<channel>commentary to=score_task <|constrain|>json<message|>{...}
    или:
    <|start|>assistant<channel>commentary to=functions.score_task<constrain>json<message>{...}

    Возвращает (tool_name, args) или None.
    """
    if not content:
        return None

    # 1) Вытаскиваем имя "to=...". Поддерживаем точку: functions.score_task
    m = re.search(r"to=([a-zA-Z_][a-zA-Z0-9_]*(?:\.[a-zA-Z_][a-zA-Z0-9_]*)?)", content)
    ALLOWED_INLINE_TOOLS = {"rag_search", "web_search", "run_code", "run_sql", "score_task"}
    if not m:
        return None

    raw_name = m.group(1)
    # если пришло functions.score_task — берём последнюю часть
    tool_name = raw_name.split(".")[-1].strip()
    if tool_name not in ALLOWED_INLINE_TOOLS:
        return None

    # 2) Вытаскиваем JSON между { ... }
    start = content.find("{")
    end = content.rfind("}")
    if start == -1 or end == -1 or end <= start:
        return None

    raw_json = content[start : end + 1]
    try:
        payload = json.loads(raw_json)
    except Exception:
        return None

    if not isinstance(payload, dict):
        return None

    return tool_name, payload

def _analyze_candidate_message(text: str) -> list[str]:
    """Detect placeholder/meta/roleplay/offtopic/empty/code_in_chat/sql_in_chat flags."""
    flags: list[str] = []
    t = text.strip().lower()
    if not t:
        flags.append("empty")
    placeholder_phrases = [
        "(отвечает правильно)",
        "(правильный ответ)",
        "код верный",
        "решение корректное",
        "ответ правильный",
        "(пишет правильный код)",
        "(solution)",
    ]
    if any(p in t for p in placeholder_phrases):
        flags.append("placeholder")
    if len(t) < 40 and ("регресс" not in t and "join" not in t and "select" not in t):
        flags.append("too_short")
    roleplay = ["представим", "я бот", "как модель", "как ассистент", "роль"]
    if any(p in t for p in roleplay):
        flags.append("roleplay")
    if "def " in t or "print(" in t or "import " in t:
        flags.append("code_in_chat")
    if "select " in t or "from " in t:
        flags.append("sql_in_chat")
    return flags

